# IRD integration (demo)

Demo endpoints:
- `GET /api/v1/tax/gst_calculation`
- `POST /api/v1/tax/gst_submission`

Goal:
- Show request/response shaping, validation, and auditability patterns that map to real integrations.
