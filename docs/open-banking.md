# Open Banking NZ (demo)

This repo is structured to be compatible with an Open Banking style API: clear resource boundaries, auth flows, and rate limiting hooks.

Demo focus:
- consistent naming
- versioned endpoints
- predictable pagination/filtering (when implemented)
