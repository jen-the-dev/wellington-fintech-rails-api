# RBNZ compliance (demo)

This project includes demo endpoints and data structures that *model* common compliance needs (audit trails, retention, reporting).

What to demo:
- `GET /api/v1/compliance/audit_logs`
- Transaction audit trails via `GET /api/v1/financial_transactions/:id/audit_trail`

Notes:
- This is a portfolio / learning repo, not production compliance software.
