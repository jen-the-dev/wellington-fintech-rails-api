# Copilot prompts for this repo

## Generate OpenAPI
- "Create an OpenAPI 3.0 spec for the endpoints in `config/routes.rb`. Keep it minimal but valid YAML."

## Add pagination
- "Update the index endpoints to support `page` and `per_page` params, and document them in `samples/requests.http`."

## Add smoke tests
- "Add request specs for `/api/v1/status`, `/health`, and a simple `financial_transactions` index."
